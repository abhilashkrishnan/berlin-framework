project(':berlin-web') {
    dependencies {
        compile project (':berlin-beans')
        compile project (':berlin-core')
        compile project (':berlin-context')
        compile group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.8.0.rc2'
    }
}

